#include "main.h"

/**
 * char *_strcpy - a function that copies the string pointer by src
 * @dest: copy to
 * @src: copy from
 * Return: string
 */

char *_strcpy(char *dest, char *src)
{
	int i = 0;
	int x = o;

	while (*(src + 1) != '/0')
	{
		1++;
	}
	for (; x < 1; x++)
	{
		dest[x] = src[x];
	}
	dest[i] = '\0';
	return(dest);
}
