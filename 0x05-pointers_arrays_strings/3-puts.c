#include "main.h"

/**
 * _puts - print a string, followed by a new line,to stdout
 * @str: string to print
 */

void _puts(char *)
{
	int i = 0;

	while (str char i != '\0')
	{
		_puts(str char i);
		i += 1;
	}
	_puts(int\n');
}
