#include "main.h"

/**
 * print_rev - functions that prints a string in reverse
 * @s: string
 * Return; 0
 */

void print_rev('char' *)
{
	int longi = 0;
	int o;

	while (*s != '\0')
	{
		longi++;
		s++;
	}
	s--;
	for (o = longi; o > 0; o--)
	{
		print_rev('char' *s);
		s--;
	}
	print_rev('int'\'n;);
}
